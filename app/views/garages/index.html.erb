<div class="container">
  <div class="row">
    <div class="col">
      <%# <h1 class="h2 my-3 text-center">Garages near by 🚘</h1> %>
      <div style="display: flex; justify-content: center;">
        <h1>Garages near by</h1>
      </div>
      <div>
        <%= form_with url: garages_path, method: :get, html: {class: "mb-2"} do |f| %>
              <%# <button class="btn btn-primary, color: white;"><%=link_to "Home", root_path(@root)%>
          <div class="d-flex justify-content-center mb-4"><%= f.label "Enter your location:" %></div>
              <%# <button type="button" class="btn btn-primary, color: white;"><%=link_to "My Vehicles", cars_path(@cars)%>
          <%#= f.text_field :query,
                           class: "form-control",
                           value: params[:query],
                           data: {
                              controller: "auto-submit",
                              action: "keyup->auto-submit#submitForm"
                            }
                           %>
        <% end %>
         <%= turbo_frame_tag "search_results" do %>
          <%# <%= render :partial => "garage", :collection => @garages, :as => :garage %>
         <% end %>
       </div>
    </div>
  </div>
</div>

<div style="width: 100%; height: 670px;"
  data-controller="map"
  data-map-markers-value="<%= @markers.to_json %>"
  data-map-api-key-value="<%= ENV['MAPBOX_API_KEY'] %>">
</div>

<%# <div> %>
  <%#=  f.input :address,
  input_html: {data: {address_autocomplete_target: "address"}},
  wrapper_html: {data: {controller: "address-autocomplete", address_autocomplete_api_key_value: ENV["MAPBOX_API_KEY"]}}
%>
<%# </div> %>
